import { DelTextDirective } from './del-text.directive';

describe('DelTextDirective', () => {
  it('should create an instance', () => {
    const directive = new DelTextDirective();
    expect(directive).toBeTruthy();
  });
});
